name: Numpy Issue

on:
  push:
    branches:
      - feature/numpy-issue
  workflow_dispatch:

jobs:
  reproduce-numpy-issue:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: false        
    - name: Set up Python 3.13
      run: uv python install 3.13
    - name: Reproduce issue.
      run: uv run --no-project --with numpy==2.0.2 -- python -c "import numpy"
